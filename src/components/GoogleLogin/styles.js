import { StyleSheet } from "react-native";
import { DARK_GREY, WHITE } from "../../utils/colors";


export const styles = StyleSheet.create({
    container : {
        backgroundColor : DARK_GREY,
        borderRadius : 14,
        width : '45%',
        alignSelf : 'center',
        alignItems : 'center',
        justifyContent : 'center',
        marginBottom : 50
    },
    image : {
        width : 30,
        height : 30,
        margin : 16
    }
}) 